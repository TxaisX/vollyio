# Handoff — sideout

_Last updated 2026-07-12. This file is the persistent project handoff — kept in-repo, updated each session._

## Goal
Sideout — volleyball skill-analysis + AI coaching web app. Next.js 16.2.10 (App Router, React 19.2), Supabase (auth + Postgres 17), Anthropic SDK server-side ("the coaching service" — never vendor-named in UI). Deployed on Vercel, repo `github.com/TxaisX/sideout`.

## State
- Git: `master`, even with `origin/master`, working tree clean. History: Phase 1a/1b + Phase 2, premium campaign, CV Phase 1 (on-device pose tracking, D-008), the focus-player pipeline, the 2026-07-11 mobile-upload fixes + free-tier onboarding funnel (D-012), score bands + overall-coherence guard, the 2026-07-12 analysis-fidelity work (D-013), the D-016 motion pass, the D-017 3D tilt/reveal, the 2026-07-12 front-page trust/legal/a11y overhaul, and the 2026-07-12 quiz-first registration funnel + four-tier levels (D-018, migration 008 applied live) all shipped. 62/62 tests, tsc clean, policy lint clean, prod build green (60 routes incl. `/start`).
- **Analysis-quality roadmap** lives in D-013's closing note. Done: fidelity phase, and **on-device ball tracking shipped 2026-07-12 (D-019)**: vendored EfficientDet-Lite0 sports-ball detector in the pose worker, cleaned timed ball path (`lib/pose/ball-track.ts`), tracked marks replace the model's eyeballed ball_track (source flips to "tracked"), measured positions feed the model as trusted input, keypoints sidecar v2 carries the path, and the clip player follows the ball live in video mode (sparse timed marks as fallback via new `result.frame_times`). Open, in order: (1) **calibration baseline** — build labeled cases from `evals/SOURCING.md` via `scripts/ingest-eval-clip.mjs`, record passRate in a new `evals/BASELINE.md`; blocked on clip acquisition (video CDNs are denied by the cloud-session network policy — user uploads or a policy allowance unblock it; note `storage.googleapis.com` IS reachable). (2) Ball-dependent measurements (toss height, set arc, contact refinement) now that the ball path exists.
- **Not yet device-verified**: the D-013 surface (scene-read line, "Rep by rep" card, fuller measurements from the full landmarker) plus the **Measured card** (breakdown now renders the raw motion-tracking checkpoints with human labels/units between Metrics and Timeline; hidden for photo/legacy rows) — run a multi-rep video analysis on the live site to confirm all of it at once. Also the **D-015 court films** (hero ambient loop + "The film room" section): confirm playback, the pause control, and reduced-motion stills on a real phone. Also the **2026-07-12 front-page overhaul** on a real phone: sticky CTA bar appears only after the hero button scrolls out, menu overlay locks scroll behind it, FAQ disclosures toggle, `/privacy` + `/terms` render, D-017 tilt/reveal-3d still feel right alongside it all. Also the **D-018 `/start` funnel** on a real phone (E2E-verified headless against the local prod build; a thumb-through of the seven steps plus signup handoff is the remaining check).
- **Deployed to production** at sideout-jet.vercel.app (Vercel project `txais-xiong-s-projects/sideout`). The Vercel project is **git-connected**: any push to `master` auto-deploys production — no CLI or specific machine required.
- **MCP servers travel with the repo** (`.mcp.json`): Supabase (project-bound) and Vercel (D-011). Any session — including claude.ai/code from a phone — gets both after a per-user OAuth grant.
  - Supabase project: **sideout**, ref `tbbievneojaxkkjvcwjp`, org `ojvxtqcefdsthcfprauv`, region us-west-2, Postgres 17, `ACTIVE_HEALTHY`.
- **`public` schema — 7 tables, RLS enabled on all:** profiles, analyses, skill_ratings, xp_events, goals, games, chat_messages.
- Cloud sessions have no `.env.local` (see `SETUP.md` section 5): use `AI_MOCK=true`, unit tests + typecheck in-session, verify on the live site post-deploy.

## Key decisions / standing rules
See `AGENTS.md` + `docs/decisions.md` D-001: no attribution trailers, no vendor names in UI (AI = "the coaching service"), design tokens locked in `app/globals.css` @theme (navy/chalk/gold/teal; Space Grotesk / Instrument Sans / IBM Plex Mono), middleware is `proxy.ts` not `middleware.ts`, dependency budget gated via the 10.5 viability gate.

## Orchestration doc gaps — CLOSED (merged to `master`)
- **`docs/backend.md`** written (Dave's data/state/platform layer). **`docs/deploy.md`** written (Thomas's deploy + CI).
- **D-004** (coaching-service model split) recorded in `docs/decisions.md`.
- **`docs/ledger.md`** Status column reconciled to its ALL-PASS header; section-10 grants set to final state.
- **CI now exists**: `.github/workflows/ci.yml` runs policy lint + tsc + test + build on push/PR (the DoD's machine floor).
- **`/learn` + `/api/eval` audited** (`docs/qa-learn-eval.md`) and fixed: metadata/boundaries/44px targets on Learn, em-dash sweep of `content/technique.ts` (0 remain), `/api/eval` fidelity + retries + error masking + citation validation.
- **Git triaged**: spec, HANDOFF, migrations now tracked; local agent tooling (`.agents/`, `.claude/`, `skills-lock.json`, `.mcp.json.bak`, the restart note) gitignored.

## Remaining loose ends (informational, not blocking)
- **`NEXT_PUBLIC_SUPPORT_EMAIL` is set in Vercel (production, 2026-07-12)** to `txiong530@gmail.com`, the owner's monitored personal mailbox. Swap it to a business address once the app gets its own domain. Preview builds intentionally keep the derived `support@<SITE_URL host>` fallback (`lib/site.ts`); the CLI's preview env add currently loops on a branch requirement, and previews carry no deletion promise.
- **In-app deletion SHIPPED 2026-07-12**: Dashboard Settings Account card, type-DELETE confirm modal, `/api/account/delete` (storage cleanup as the user, then `delete_own_account()` security-definer RPC from migration `009_delete_account.sql`, applied live). Privacy Policy wording updated (self-serve immediate + email fallback). E2E-verified: account, rows, and storage objects all zero after the flow.
- **Legal copy is honest but unreviewed** — `/privacy` and `/terms` were written against actual code behavior (frames to the coaching service, clips in account-private storage, opt-in training default-off, 13+ floor). Have counsel skim both before any marketing push.
- **Social proof still absent by design** — no testimonials were fabricated (FTC). The FAQ + example-data labels + legal pages carry trust for now; drop in one real coach/player quote when one exists.
- **Film-room loop idles dark** for part of its cycle — re-render via `scripts/render-hero-film.mjs` so the overlays/score stay on screen most of the loop and the poster frame shows the read.
- **Migration tracking still empty** (`list_migrations` → `[]`) — schema applied out-of-band; documented in `backend.md`. A future migration workflow must account for it.
- **`005_clips.sql` still NOT applied** — no `clips` table live (file now tracked). Apply when the clip feature ships.
- **Two `004_*` migrations** (`004_discipline.sql`, `004_xp_events_index.sql`) — order-independent; fold into one ordinal if the sequence is ever rebased.
- **Numeric Lighthouse not in CI** (tool not installed); verified manually 99/100. Optional `@lhci/cli` job later.

## Current next step
Device-verify the front-page overhaul + D-013 analysis surface (multi-rep clip → scene read + Rep by rep card) and get counsel eyes on `/privacy` + `/terms`, then continue the analysis-quality roadmap in the State section (calibration baseline → ball tracking). Still open from before: funnel conversion watching; monetization (D-012 premium seam via `canAnalyze` + `BILLING_ENABLED`); deferred features (Overall Game option, onboarding motion-tracking demo, launch-teaser remix — teaser parked in `assets/sideout-launch-teaser.mp4`, not served).

## Run / deploy quickref
`npm run dev` (:3000) · `npm run lint` · `npm run typecheck` · `npm test` · `npm run build`. Push to `master` deploys production; `vercel deploy [--prod]` only for ad-hoc previews. Build hits `EPERM: unlink .next/...` (OneDrive lock, local machine only) → `Remove-Item -Recurse -Force .next` and rebuild.

---

## Session log
- **2026-07-13 (clip trim window)** — Owner asked for a cropping tool to shorten the window and sharpen what gets analyzed; question round chose a time trim over a spatial crop. Shipped: a two-handle TrimBar on the framing card (gold span = analyzed window; pointer drag with nearest-handle pick, arrow keys with Shift steps, sliding-window behavior so dragging past the 45s cap pulls the other handle along and the window is valid at every instant); the scrubber and the confirmed framing moment clamp inside the window; `ExtractOpts.window` bounds every sampling path in `lib/frames.ts` (probes, dense capture, frame plan, extras, uniform fallback) via shared `clampTrimWindow`/`MIN_TRIM_SPAN_S` in `lib/frame-select.ts`; and **clips longer than 45s are no longer rejected** — the framing card opens for any duration and the trim (default first 45s, capped) makes them analyzable. Times stay absolute clip seconds so the stored clip, sidecar, and player line up unchanged. Gates: policy lint, tsc, 79 tests (3 new on the window math), prod build. E2E on the local prod build with a SQL-confirmed throwaway (deleted via the real /api/account/delete, 200): 8s clip shows defaults + keyboard (8s -> 7s) + drag (4.9s) all live; 60s clip opens the card with a capped 45s window and the "Only the gold window is analyzed" hint. NOTE: signup now lands on "Check your email" again — Confirm email appears re-enabled on the Supabase project (it was disabled 2026-07-12); worth deciding which state is intended. Same session, owner follow-up: **the automated player identifier is back in front** — an untouched frame hands the pick to the activity ranking across the trimmed window (primary button reads "Find my player and analyze"; touching the frame or tapping a candidate pins that player and flips the label to "Analyze this athlete"); card copy leads with trim-and-go. E2E re-run covered the label flip on first touch. SHIPPED as `4e5651c`, deployed and alias-verified live.
- **2026-07-13 (framing is always the user's + court overlays)** — Owner asked to always choose the frame and the player, with automation following from that choice, and for the analysis to capture everyone playing while measuring only the POI (overlays-only, chosen via question round). Shipped: the framing card now opens for every video clip even when the pose engine fails to load or detects nobody (`detectOpeningPlayers` accepts a null engine and renders the opening frame anyway; the drawn frame anchors tracking on its own); the keypoints sidecar carries the non-followed players' timed bounding boxes (`others`, additive on v2; new pure helpers `personBox`/`otherTrackBoxes`/`boxAtTime` in `lib/pose/kinematics.ts`, the component's duplicate `boxFromPts` deleted in favor of the lib version); the results clip player draws those boxes dim at the playhead under the gold POI skeleton, with the trace label noting "other players boxed". Also this session: Analyze's captured-frames grid shows full uncropped frames at native aspect ratio in a tighter 3/4-column grid (`components/filmstrip.tsx`). Gates: policy lint, tsc, 76 tests, prod build. SHIPPED: `e77e537` deployed and alias-verified live. Same session, owner request: **web analytics added (D-020)** — `@vercel/analytics@2.0.1` through the 10.5 gate, `<Analytics />` mounted in the root layout, shipped as `b755c12`, alias-verified; `/_vercel/insights/script.js` serves 200 on the live domain (Web Analytics is enabled on the project), data appears in the Vercel dashboard's Analytics tab. Verify on device: upload a multi-player clip with the engine cold to see the card still open; on results, dim boxes should ride the other players while the skeleton stays on the POI.
- **2026-07-12 (signup clickwrap)** — Terms now bind users: signup requires an affirmative checkbox (at least 13, agrees to Terms + Privacy, guardian consent under 18, links open in new tabs), enforced server-side in the signup action (not just browser validation), with `terms_accepted_at` stamped into the auth user metadata as acceptance evidence. E2E: submit blocked while unchecked; checked signup creates the user with the timestamp in `raw_user_meta_data`. Gates: tsc, lint, 73 tests, build.
- **2026-07-12 (in-app deletion)** — The deletion promise became self-serve: Account card gains a Delete account block (btn-destructive, type-DELETE modal), `/api/account/delete` removes both storage buckets' own-folder files then calls `delete_own_account()` (SECURITY DEFINER, migration 009, applied live out-of-band); profiles cascades from auth.users and all seven tables cascade from profiles (verified in pg catalogs), so one call wipes everything. Privacy retention section now names the in-app path with email as fallback. E2E on the local prod build: throwaway signed up, analyzed (real storage files), deleted through the UI, bounced to login afterward; DB shows 0 auth user / 0 rows / 0 storage objects. Gates: tsc, lint, 73 tests, build.
- **2026-07-12 (90% person-identification bar + always-choosable)** — People are OFFERED as tappable candidates confident-first: `personConfidence` (mean visibility of the four torso anchors) with a 0.9 bar, and `offerPersons` falls back to the best available detections when nobody clears it, so the player can always choose who gets analyzed (owner requirement); the drawn framing box stays the final override. Applied to the opening-player picker (`detectOpeningPlayers`) and the live scrub candidates. Deliberately NOT applied to the follow pipeline (hard footage still measures) or to snapping the user's drawn box to a body (their framing is the confidence there). Gates: tsc, lint, 73 tests, build.
- **2026-07-12 (ball tracking, D-019)** — Prompted by "ball follow works indoor but not beach": diagnosis showed discipline plays no role (both rows had near-identical model ball data); the gold ball marker only existed in the frame-by-frame fallback, and video mode never drew a ball at all. Shipped the real fix, D-013's P2: vendored EfficientDet-Lite0 (float16, sha256-pinned, 10.5-gated in D-019) as a sports-ball ObjectDetector alongside the pose landmarker in both engine paths; full-frame ball bitmaps under player crops; `lib/pose/ball-track.ts` (confidence floor 0.2 calibrated on the repo's own volleyball photo, teleport gate with re-lock, dedupe, frame projection with crop mapping, playhead interpolation) with 8 node tests; capture collects probe + dense detections; request carries tracked marks when the path is real (8+ points, 3+ on sent frames); server swaps them in with `ball_track_source: "tracked"`, stores `frame_times`, and hands the measured positions to the coaching service as trusted input; keypoints sidecar is v2 with the timed path; ClipPlayer follows the ball at the playhead (dense interpolated, sparse timed fallback, measured-vs-estimated label) and FramePlayer stays as was. Gates: tsc, policy lint, 70 tests, prod build; real-Chromium WASM smoke detected "sports ball" on the actual volleyball photo. Verify on device with a real rep: ball ring should ride the clip in video mode, and the trace label should read "measured ball" once the detector genuinely followed it.
- **2026-07-12 (one-time consent question fixes)** — The training-consent question on Analyze had three silent failure paths: (1) the submit gate checked `consentAnswered === false` while the answered-check was still in flight (`null`), so a submit during that window skipped the question entirely (wide window on phones); (2) the queued submit ran against a stale closure after answering, re-tripping the gate and reopening the modal; (3) a failed browser write (supabase-js returns errors, RLS blocks are zero-row successes) was indistinguishable from a save. Fixed: the gate lives in a ref and queues in-flight submits (6s fail-open if auth hangs), the write is read back and warns on failure, and a dead session (deleted user, valid JWT) signs out to /login instead of failing open. E2E-verified on the local prod build with AI_MOCK: modal appears on first analysis, Allow proceeds and persists, no modal on the second analysis; throwaway deleted. Also this session: signup maps the Supabase email rate-limit error to plain copy; owner disabled Confirm email in the dashboard (signups no longer send mail), and the owner's real account now exists (txiong530@gmail.com).
- **2026-07-12 (quiz-first funnel, D-018)** — The registration funnel now starts before the account: public `/start` quiz (discipline, level, position, play frequency, focus skill, target + 30/90/180-day timeframe, echo), answers parked in localStorage and applied by `FunnelHandoff` + `applyFunnel` on the first authed page (profile fields + goal with chosen deadline + redirect to `/analyze?skill=&discipline=`), landing CTAs repointed at `/start`, `/welcome` kept as the same-steps fallback. Level tiers renamed beginner/intermediate/expert/pro across DB (migration `008_funnel.sql`, applied live out-of-band), code, drills, and UI; `profiles` gains `discipline`/`position`/`play_frequency`; per-level coaching voice in the analysis output spec and coach chat, with pro deliberately harsh (professional standard, unsoftened verdicts). Gates: tsc, policy lint, 62 tests, 60-route prod build; funnel E2E-verified against the local prod build with a throwaway account (created, SQL-confirmed, applied, deleted; DB rows + `/analyze` landing confirmed). Also removed the empty leftover `app/launch/` dir that broke tsc via stale `.next` types.
- **2026-07-12 (support mailbox)** — `NEXT_PUBLIC_SUPPORT_EMAIL` set to `txiong530@gmail.com` on the Vercel production environment (owner's monitored personal mailbox; a business address replaces it when the app gets its own domain). Var added to `.env.example`. This push rebuilds production so the build-time value lands on `/privacy`, `/terms`, and the footer Contact link; verified live post-deploy.
- **2026-07-12 (front-page review → trust/legal/a11y overhaul)** — Owner asked for a brutal UX review of the landing page, then to fix everything with business/legal tightness as the goal. Review ran against code + live browser passes (1440px, 390px, reduced-motion, console, tab order); all 19 findings then fixed and merged to `master` (`f352612`, a real merge over the parallel D-017 tilt work — the merge re-gated `reveal-3d`'s hidden pre-state behind `:root.js` so it wouldn't reintroduce the no-JS bug). Shipped, by theme: **defects** — `useReducedMotion` settles post-mount (hydration error for reduce users gone); `.reveal`'s hidden state is JS-gated via an inline script stamping `js` on `<html>`, so the whole page renders without JavaScript. **Legal/trust** — `/privacy` + `/terms` in a new `(legal)` route group, written against actual code behavior (13+ floor, guardian consent for minors, opt-in training default-off, contact-based deletion within 30 days, AI-accuracy + assumption-of-risk clauses, no vendor names); footer became link columns (Product / Legal / Account + Contact mailto); sitemap updated; `SUPPORT_EMAIL` added to `lib/site.ts` (env `NEXT_PUBLIC_SUPPORT_EMAIL`, fallback derived from `SITE_URL`); all demo numbers labeled example data; **no fabricated social proof** (FTC) — trust is carried by FAQ + policies until a real quote exists. **Conversion** — one CTA label everywhere ("Analyze your first rep"); new `components/sticky-cta.tsx` shows the mobile bottom bar only after the hero CTA scrolls out (IO on `#hero-cta`); `#faq` section (native `details`/`summary`, 5 concrete answers: devices, cost, film handling, accuracy, rating) + nav link; final section carries a readable positioning line + privacy reassurance. **Hero/mobile** — the value prop is the H1 (wordmark repeat cut); a third `hero-film-shade` layer calms the film's ball graphic under the nav; eyebrow trimmed on phones; hero pause control moved clear of the chips; Magnetic removed from primary CTAs; mobile menu is now a full-height overlay with scroll lock + Tab containment (Escape restores focus to the toggle). **Legibility/copy** — 11px floor on the landing (was 9–10px); the unpausable marquee deleted (keyframe + token too); rep tabs abbreviate (Base/Focus/Latest) instead of bare digits; "ten seconds" vs "under a minute" conflict reworded. Gates: policy lint (em-dash sweep of new copy included), tsc, 62 tests, prod build (+2 static routes), a 16-check Playwright suite (desktop / mobile / reduced-motion / JS-disabled / menu / sticky gating / legal pages) plus a 4-check post-merge re-verify — all console-clean. Verify on device per the State section; then set the support mailbox and get counsel eyes on the legal copy.
- **2026-07-12 (in-house skills)** — `.claude/skills/` is now tracked (gitignore carve-out; local agent files stay ignored) and carries three Sideout-authored skills that load in every session: `ui-animation` (10.2 ground rules), `animation-designer` (pattern recipes), `css-animations` (pure-CSS technique book). Written in-house instead of installing marketplace equivalents so the guidance is pre-fitted to the repo constitution; Flutter/Three.js guidance deliberately not carried (D-016).
- **2026-07-12 (motion pass, D-016)** — Owner shared six community "animation skills" and granted full creative access. Their registry is unreachable from this environment and they're instruction docs, not code, so the craft was applied by hand inside 10.2 (D-016 records the reasoning, including declining Flutter — wrong platform — and Three.js — fails the 10.5 necessity gate). Shipped: the scoring wait becomes "the coaching read" (a `StatusTicker` walking the real pipeline stages, resting on the last line, + a `.scan-line` gold sweep over the filmstrip; ticker carries the state so reduced motion just hides the band), a pure-CSS scroll-driven reading-progress hairline on the breakdown (`animation-timeline: scroll(root)`, `@supports`-gated, kept under reduced motion since it mirrors user scrolling), skeleton shimmer sweep for every `loading.tsx`, and a gold focus glow on `.input-field`. No new deps/colors/fonts. Gates: tsc, policy lint, 62 tests, build; scroll-timeline math verified in headless Chromium. Verify on device: upload a rep and watch the ticker + scan during scoring; scroll a breakdown for the top hairline.
- **2026-07-12 (account card)** — Dashboard Settings gains an Account card: player name + email, a plain **Sign out** button (sign-out previously lived only in the nav chrome), and coaching-level chips — the first post-onboarding writer of `profiles.level` (same column the welcome quiz sets and the analysis prompts read). Rebased over the D-015 court-films work from the parallel session.
- **2026-07-12 (track continuity, D-014)** — Player Lock spec reviewed: state-machine/continuity ideas adopted on-device (`lib/pose/track-state.ts`: evidence-only occlusion/exit labeling, coverage, lost flag; `tracked_coverage` + `frame_exits` session stats to the model; one-line follow note in the analyze preview), hosted-segmentation spine deferred until billing (full disposition in D-014). Continuity also travels the wire now (`AnalyzeRequest.continuity` → validated → stored on the result) and both results players draw playhead-synced absence chips (dashed gold edge chip with direction arrow for exits, bottom chip for occlusions). Gates: tsc, lint, 62 tests, build.
- **2026-07-12 (retire the still photo + launch film)** — Owner asked for the old hero photo and launch video to come off the site now that the D-015 court films supersede them. Removed: the `/launch` route, `components/launch-film.tsx`, `public/sideout-launch.mp4`, `scripts/render-launch-video.mjs`, the launch page block in `scripts/build-sites.mjs`, and every `launch-*` / `hero-photo` / `hero-shade` / `auth-photo-shade` / `hero-hud` / `hero-metric-fill` rule and keyframe in `globals.css`. Login/signup swapped the photo backdrop for the photo-free glow + drifting seam arcs; the hero's ghost button now anchors to `#film` ("Watch a rep get read"). `public/volleyball-hero.webp` stays in the repo strictly as the `/film` capture plate (no user-facing page renders it); the asset register records both changes. The parked vertical teaser in `assets/` was never served and is untouched.
- **2026-07-12 (court films, D-015)** — Shipped the landing page's volleyball motion without touching a stock library: a capture-only `/film` route (noindexed, robots-disallowed) stages a 10s seamless "court vision" loop over the D-007 hero photo (scan sweep, pose-skeleton trace locked to the player, projected ball path, measured checkpoints in real product units, score 82 + priority fix), and `scripts/render-hero-film.mjs` captures it deterministically (Web Animations API currentTime stepping over CDP, 300 frames at 30fps, loop seam verified byte-identical). Two takes ship in `public/`: `sideout-hero-loop` (ambient) now plays full-bleed behind the hero copy (the old HUD card retired; `objectPosition` keeps the player in frame at 390px), and `sideout-court-vision` anchors a new "The film room" landing section (+ nav anchor). Each take is MP4 + smaller VP9 WebM + WebP poster, played by `components/court-film.tsx`: muted/looped/`preload="none"`, poster-first, plays only in view, visible pause control (WCAG 2.2.2), still poster under reduced motion (verified: 0 videos in DOM) and data-saver. Assets registered in `docs/assets.md`; decision recorded as D-015. Gates: policy lint, tsc, 56/56 tests, prod build; verified by screenshot at desktop + 390px, playback probe (t advancing on VP9), reduced-motion probe. Note: local headless Chromium lacks H.264, so in-container playback checks use the WebM path; real browsers take H.264 or VP9. Verify on device post-deploy: hero loop plays and pauses, film-room section plays on scroll, reduced-motion phones get stills.
- **2026-07-12 (player framing)** — The POI dot became a sizeable player frame: drag to move, corner handles to resize, arrow keys + Shift-resize, auto-framed candidate boxes on every detected person (tap to switch), live re-detection as the user scrubs (debounced; auto-snap stops after first manual touch). Reframe-miss root causes fixed: the framed moment always gets a dense capture window, and matching moved to `pickTargetTrack` (box-scaled tolerance, in-frame acceptance, still nobody-over-wrong-player). Also: the breakdown's Measured card (raw checkpoints, human units). Gates: tsc, lint, 56 tests, build. Verify on device: frame each of two players in a multi-player clip and confirm the right one gets measured.
- **2026-07-12 (analysis fidelity, D-013)** — Full pose landmarker added with lite fallback (engine reports loaded variant); real-timestamp monotonic clock replaces the synthetic 30fps tick in the pose worker; model contract gains optional `scene_read` + `rep_scores` (rendered on the breakdown: coach's opening line + "Rep by rep" card with spread); new measured checkpoints `knee_flexion_at_plant` + `shoulder_hip_separation`. Also shipped earlier same day: score bands + coherence guard, eval ingest CLI + `evals/SOURCING.md`. Gates: tsc, policy lint, 52 tests, 57-route build. Next for analysis quality: calibration baseline (`evals/BASELINE.md`) once clips can be downloaded, then on-device ball tracking (P3 in D-013's roadmap; hooks already in place).
- **2026-07-11 (funnel)** — Competitive teardown of two consumer coaching apps (frame-by-frame screen recordings + store listings), then shipped D-012: `/welcome` onboarding (level · focus · target · echo; first writer of `profiles.level`; creates a goal), signup → `/welcome`, `/analyze?skill=` preselect, breakdown value chips/#1-fix kicker/closing share card. Deferred: billing (seam recorded in D-012), Overall Game option, onboarding CV demo. Gates: tsc, policy lint, 48 tests, 57-route build. **Verified live on device 2026-07-11**: signup → welcome quiz → analyze (skill preselected) → breakdown, plus the gallery-upload and extra-frames fixes. A 13s vertical launch teaser (real app screens, synthesized soundtrack) sits in `assets/sideout-launch-teaser.mp4` — repo-only, deliberately not in `public/`, so nothing on the live site serves it.
- **2026-07-11 (mobile upload fixes)** — First cloud session from claude.ai/code. Fixed Android jumping to the camera instead of the gallery on "Upload a clip" (wildcard `accept` → explicit MIME list; frame-by-frame screen-recording diagnosis confirmed the user had tested a frozen `…projects.vercel.app` deployment URL — production URL is the one to verify on). Fixed `/api/analyze` 400 "Bad request." on gallery clips: the planner stores up to 24 frames so `extra_frame_count` legitimately exceeds 12 when the send set is small; schema now bounds by shared `MAX_STORED_FRAMES`. Photo picker capped at 12 with a friendly error. 48/48 tests, tsc, policy lint, build green; deployed via push to `master` (`cc30a0a`).
- **2026-07-10 (mobile enablement)** — Prepped the repo for phone/cloud work: added the Vercel MCP server to `.mcp.json` (D-011), tracked `.env.example`, rewrote SETUP.md (auto-deploy + agent-sessions section) and this handoff to current state.
- **2026-07-10 (focus player)** — Iterated the focus-player pipeline to a head-anchored POI dot with region-zoomed tracking, framed crops, and start-time-bounded analysis (`components/analyze-flow.tsx` and friends, bdd6414 → 0c89dc6). 47/47 tests, tsc, policy lint, prod build, Playwright smoke all green; deployed to production.
- **2026-07-10 (premium campaign)** — Generated and registered a 63.1 KiB volleyball hero, rebuilt landing/login/signup, refined app-shell/dashboard hierarchy, and added one-time campaign motion. Desktop, exact 390px/360px, reduced motion, lint, tsc, 18/18 tests, and 55-route build pass. Not deployed.
- **2026-07-09 (UI motion)** — Fixed Learn's fragmented inline cards, added shared reward feedback to milestone surfaces, and contained the mobile tab bar. Verified with desktop, exact 390px, and exact 360px browser metrics; reduced motion, lint, tsc, 18/18 tests, and 55-route build pass.
- **2026-07-09** — Re-ran the perfection prompt as a fresh audit against completed `master`. Verified all project-owned source and orchestration artifacts, closed reduced-motion/copy/lint enforcement gaps, and kept the 21-component + route ledger all-pass. Gates: policy lint, tsc, tests 18/18, build 55 routes.
- **2026-07-08** — Verified Supabase MCP connectivity (resolved stale-connection bug), inventoried the 7 public tables + migrations, established this in-repo handoff. Read-only; no source edits.
- **2026-07-08 (2)** — Closed the orchestration doc + CI gaps (see section above): wrote backend.md/deploy.md/qa-learn-eval.md, recorded D-004, reconciled ledger, added CI, audited + fixed `/learn` and `/api/eval`, git-triaged untracked files. Gates green (tsc / test 18-0 / build 55 routes). Committed to branch `docs/close-orchestration-gaps` (3306212); not pushed/merged.

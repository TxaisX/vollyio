# Asset Register (section 10.3 / 10.4)

Every sourced or generated volleyball visual lands here before it ships.
Required per asset: file path · source (URL/tool) · license + rights-clearance for this use · dimensions/format · optimized (next/image or equiv, lazy off-screen, no CLS) · reduced-motion behavior (still poster fallback if it moves) · alt text (project voice, from Lisa) or `aria-hidden` if decorative · on-token chrome confirmed (10.1).

Prefer the fewest, best assets over volume. Assets that fight the navy/gold world or read as stock filler do not ship.

| Asset | Source | License | Dims/Format | A11y (alt / aria-hidden) | Reduced-motion | Verified (Sierra) |
|---|---|---|---|---|---|---|
| `public/volleyball-hero.webp` | Built-in image generation, created specifically for Sideout; prompt recorded in D-007 | Generated original with no third-party source, logos, or watermark; cleared for project use | 1774×888 WebP, 63.1 KiB | **Capture source only since 2026-07-12**: no user-facing page renders the still photo anymore (auth pages went photo-free; the landing shows the D-014 films built from it). It stays in `public/` as the `/film` route's base plate for re-renders | n/a on user surfaces; inside `/film` it sits under the scene's own reduced-motion settle | PASS 2026-07-10 (original inspection); usage narrowed 2026-07-12 |
| `public/sideout-launch.mp4` | ~~Project-local launch route rendered from the Sideout UI~~ | — | — | — | — | **REMOVED 2026-07-12** with the `/launch` route and `components/launch-film.tsx` at the owner's request; superseded by the D-014 court films |
| `public/sideout-hero-loop.mp4` + `.webm` + `-poster.webp` | Rendered from the project's own `/film?variant=ambient` route (D-007 hero photo + project CSS/SVG overlays) via `scripts/render-hero-film.mjs`; no third-party footage | Original project composition, no third-party source, logos, or watermark; cleared for project use | 1280×720, 10.0s seamless loop, 30fps; H.264 MP4 842 KiB + VP9 WebM 246 KiB (listed first) + WebP poster 25 KiB | Decorative hero backdrop: video `aria-hidden`, poster served with empty alt; the copy beside it carries the message | JS matchMedia guard swaps the video for the still poster; data-saver gets the poster too; visible pause control on the instance | PASS 2026-07-12: loop seam byte-identical (frame 300 = frame 0), desktop/390 framing, playback probe (VP9), reduced-motion probe (0 videos in DOM) |
| `public/sideout-court-vision.mp4` + `.webm` + `-poster.webp` | Rendered from the project's own `/film` route via `scripts/render-hero-film.mjs`; checkpoint labels/units mirror `lib/pose/measurement-format.ts` | Original project composition, no third-party source, logos, or watermark; cleared for project use | 1280×720, 10.0s seamless loop, 30fps; H.264 MP4 921 KiB + VP9 WebM 315 KiB (listed first) + WebP poster 39 KiB | "The film room" landing section: video carries a full descriptive `aria-label`; poster alt describes the traced serve and score | Same CourtFilm rules: matchMedia reduced-motion guard settles on the poster, data-saver gets the poster, visible pause control | PASS 2026-07-12: loop seam byte-identical, story beats inspected frame-by-frame (scan/skeleton/tracer/chips/score/fix), in-section playback screenshot |

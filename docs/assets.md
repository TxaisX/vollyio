# Asset Register (section 10.3 / 10.4)

Every sourced or generated volleyball visual lands here before it ships.
Required per asset: file path · source (URL/tool) · license + rights-clearance for this use · dimensions/format · optimized (next/image or equiv, lazy off-screen, no CLS) · reduced-motion behavior (still poster fallback if it moves) · alt text (project voice, from Lisa) or `aria-hidden` if decorative · on-token chrome confirmed (10.1).

Prefer the fewest, best assets over volume. Assets that fight the navy/gold world or read as stock filler do not ship.

| Asset | Source | License | Dims/Format | A11y (alt / aria-hidden) | Reduced-motion | Verified (Sierra) |
|---|---|---|---|---|---|---|
| `public/volleyball-hero.webp` | Built-in image generation, created specifically for Vollyio; prompt recorded in D-007 | Generated original with no third-party source, logos, or watermark; cleared for project use | 1774×888 WebP, 63.1 KiB | **Unused since 2026-07-14 (D-022)**: was the `/film` base plate; the films now build on `film-court.webp` (a real two-player rep) to match the D-021 multi-player tool. **Deleted from `public/` on 2026-08-27**: it had rendered on no page for six weeks, and a file nothing serves is a question a future reader has to ask when git already holds the answer. Recover with `git show <pre-deletion-sha>:public/volleyball-hero.webp` if the plate is ever wanted again | n/a | PASS 2026-07-10 (original); retired as plate 2026-07-14 |
| `public/film-court.webp` | Frame 0225 of the project's own volleyball calibration clip (`D:\posecmp\frames_raw`, the clip used to validate the D-021 pose engine), graded via ffmpeg | **RIGHTS CAVEAT**: real, identifiable people (owner's own "beach test clip", chosen by the owner for this rebuild). Owner must confirm likeness consent for commercial marketing use before this ships publicly — unlike the synthetic D-007 plate, this depicts real individuals | 720×1280 portrait WebP, 240 KiB | Capture source only: the `/film` route's base plate. No user page renders it directly; it appears only inside the rendered loop | n/a on user surfaces | PENDING owner likeness confirmation |
| `public/vollyio-launch.mp4` | ~~Project-local launch route rendered from the Vollyio UI~~ | — | — | — | — | **REMOVED 2026-07-12** with the `/launch` route and `components/launch-film.tsx` at the owner's request; superseded by the D-015 court films |
| `public/vollyio-hero-loop.mp4` + `.webm` + `-poster.webp` | Rendered from `/film?variant=ambient` (real `film-court.webp` plate + project CSS/SVG overlays baked from REAL RTMPose output) via `scripts/render-hero-film.mjs`; no third-party footage | Project composition over the owner's own clip; see `film-court.webp` likeness caveat | 1280×720, 10.0s seamless loop, 30fps; H.264 MP4 375 KiB + VP9 WebM 341 KiB (listed first) + WebP poster ~30 KiB | Decorative hero backdrop: video `aria-hidden`, poster empty alt; the copy beside it carries the message | JS matchMedia guard swaps video for still poster; data-saver too; visible pause control | PASS 2026-07-14 (D-022): loop seam byte-identical, hero framing desktop + 402px, in-page playback |
| `public/vollyio-court-vision.mp4` + `.webm` + `-poster.webp` | Rendered from `/film` via `scripts/render-hero-film.mjs`; skeleton + boxes are REAL RTMPose keypoints for a two-player rep, checkpoint labels/units mirror `lib/pose/measurement-format.ts` | Project composition over the owner's own clip; see `film-court.webp` likeness caveat | 1280×720, 10.0s seamless loop, 30fps; H.264 MP4 450 KiB + VP9 WebM 403 KiB (listed first) + WebP poster 57 KiB | "The film room" landing section: video carries a full descriptive `aria-label`; poster alt describes the two-player read and score | Same CourtFilm rules: matchMedia reduced-motion guard settles on the poster, data-saver gets the poster, visible pause control | PASS 2026-07-14 (D-022): loop seam byte-identical, story beats inspected (scan/boxes/watching/skeleton/ball/chips/score/fix), in-section playback screenshot |
